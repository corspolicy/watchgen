import { Request, Response } from "express";

export default async function ListUsers(req: Request, res: Response) {
  // TODO: /anime/list uçnoktasında yaptığımız gibi bir pagination sistemi kullanarak kullanıcıları filtrele
}
