import { Request, Response } from "express";

export default async function GetUser(req: Request, res: Response) {
  // TODO: Seçili kimliğe sahip kullanıcıyı veritabanından alıp geri döndürecek bir kod yaz
}
